<Window x:Class="WpfPanel.View.UI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:converters="clr-namespace:WpfPanel.Utilities.Converters"    
        xmlns:vm="clr-namespace:WpfPanel.ViewModel"        
        mc:Ignorable="d" 
        d:DesignHeight="350" d:DesignWidth="300" Height="750" Width="300">
    <Window.Resources>
        <Style x:Key="circuitButton" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Annotation}" Value="{x:Null}">
                    <!--<Setter Property="Content" Value="{Binding Name}"/>-->
                    <Setter Property="Content" Value="N"/>
                </DataTrigger>
            </Style.Triggers>
            <Setter Property="Width" Value="20"/>
            <Setter Property="BorderThickness" Value="0"/>
            <!--<Setter Property="Content" Value="qwerty"/>-->
            <Setter Property="Content">
                <Setter.Value>
                    qwerty
                    <!--<Image Source="{Binding Annotation}"/>
                    <Image Source="/Resources/icons8-Create-24.png"/>-->
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="circuitTextBlock" TargetType="TextBlock">
            <Setter Property="Margin" Value="0 0 100 0"/>
        </Style>
    </Window.Resources>
    <Grid Background="White" x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" ItemsSource="{Binding Circuits}" x:Name="apartmentCircuits">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Number}" 
                                   x:Name="circuitNumber"
                                   Style="{StaticResource circuitTextBlock}"/>
                        <ListView ItemsSource="{Binding ApartmentElements}" x:Name="el">
                            <ListView.Resources>
                                <converters:ElementDataConverter x:Key="ElementDataConverter"/>
                            </ListView.Resources>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <!--Style="{StaticResource circuitButton}"-->
                                    <Button 
                                        Tag="{Binding Category}"
                                        KeyDown="Button_KeyDown"
                                        MouseEnter="Button_MouseEnter"
                                        MouseLeave="Button_MouseLeave"
                                        Command="{Binding DataContext.InsertElementCommand,
                                            RelativeSource={RelativeSource AncestorType=Window}}">
                                        <Button.CommandParameter>
                                            <MultiBinding Converter="{StaticResource ElementDataConverter}">
                                                <Binding Path="Text" ElementName="circuitNumber"/>
                                                <Binding Path="Name"/>
                                                <Binding Path="Category"/>
                                            </MultiBinding>
                                        </Button.CommandParameter>
                                        <!--<Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Image Source="{Binding Annotation}"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>-->
                                        <Image Source="/Resources/icons8-Create-24.png"/>
                                        <!--<Image Source="{Binding Annotation}"/>-->
                                    </Button>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Grid.Row="1" 
                Command="{Binding ConfigureCommand}"
                Content="Config" 
                Width="50" 
                HorizontalAlignment="Right" 
                Margin="0 0 40 0" />
        <WrapPanel Grid.Row="2">
            <TextBlock Text="Current suffix of circuit: "/>
            <TextBlock
                    Text="{Binding CurrentSuffix}">
                <TextBlock.InputBindings>
                    <MouseBinding 
                        x:Name="currentSuffix" 
                        Command="{Binding SetCurrentSuffixCommand}"/>
                </TextBlock.InputBindings>
            </TextBlock>
        </WrapPanel>
        <WrapPanel Grid.Row="3">
            <TextBlock Text="Height"/>
            <TextBox Text="{Binding Height}" Margin="5 0 0 0" Width="30"/>
        </WrapPanel>
        <StatusBar Grid.Row="4">
            <StatusBarItem x:Name="statusBarItem" Content="{Binding Status}">
                <StatusBarItem.Style>
                    <Style TargetType="StatusBarItem">
                        <Setter Property="Background" Value="Orange"></Setter>
                        <Style.Triggers>
                            <Trigger Property="Content" Value="{x:Null}">
                                <Setter Property="Background" Value="Azure" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </StatusBarItem.Style>
            </StatusBarItem>
        </StatusBar>
        <WrapPanel Grid.Row="5" Visibility="Visible">
            <Button Content="savingConfig" x:Name="latestConfig" Command="{Binding SaveLatestConfigCommand}"/>
            <Button Content="loadingConfig" Command="{Binding LoadLatestConfigCommand}"/>
            <Button Content="test" Click="Button_Click"/>
        </WrapPanel>
        <ListView Grid.Row="6" ItemsSource="{Binding Circuits}" x:Name="testLVOuter">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListView ItemsSource="{Binding ApartmentElements}" x:Name="testLVInner">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <WrapPanel>
                                    <!--<TextBlock Text="{Binding Name}"/>-->
                                    <Button>
                                        <!--<Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <WrapPanel>
                                                    <TextBlock Text="{Binding Name}"/>
                                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                                </WrapPanel>
                                            </ControlTemplate>
                                        </Button.Template>-->
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Image Source="{Binding Annotation, ElementName=testLVInner}" Height="20"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <!--<Image Source="/Resources/icons8-Create-24.png"/>-->
                                </WrapPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
