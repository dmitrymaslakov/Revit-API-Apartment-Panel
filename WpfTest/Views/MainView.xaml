<Window x:Class="WpfTest.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        xmlns:vm="clr-namespace:WpfTest.ViewModels"
        xmlns:converters="clr-namespace:WpfTest.Utils.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=True}"
        Title="Testing WPF" 
        Height="450" Width="800">
    <Window.Resources>
        <converters:HeightDataConverter x:Key="heightDataConverter"/>
    </Window.Resources>
    <Grid>
        <GroupBox Grid.Row="3" Header="Height">
            <UniformGrid Columns="3">
                <RadioButton GroupName="TypeOfHeight"
                             Command="{Binding SetHeightCommand}"
                             CommandParameter="{Binding ElementName=listHeightsUK, Path=SelectedItem}">
                    <!--<RadioButton.CommandParameter>
                        <MultiBinding Converter="{StaticResource heightDataConverter}">
                            <Binding Path="Text" ElementName="heightTypeOfUK"/>
                            <Binding Path="SelectedItem" ElementName="listHeightsUK"/>
                        </MultiBinding>
                    </RadioButton.CommandParameter>-->
                    <StackPanel>
                        <TextBlock x:Name="heightTypeOfUK" Text="UK"/>
                        <ListView x:Name="listHeightsUK" ItemsSource="{Binding ListHeightsUK}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton GroupName="UK" Content="{Binding}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBox/>
                    </StackPanel>
                </RadioButton>
                <RadioButton GroupName="TypeOfHeight">
                    <StackPanel>
                        <TextBlock Text="OK"/>
                        <ListView ItemsSource="{Binding ListHeightsOK}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton GroupName="OK" Content="{Binding}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBox>
                            <TextBox.InputBindings>
                                <KeyBinding Command="{Binding }"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </StackPanel>
                </RadioButton>
                <RadioButton GroupName="TypeOfHeight">
                    <StackPanel>
                        <TextBlock Text="Center"/>
                        <ListView ItemsSource="{Binding ListHeightsCenter}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton GroupName="Center" Content="{Binding}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBox/>
                    </StackPanel>
                </RadioButton>
            </UniformGrid>
        </GroupBox>
    </Grid>
</Window>
